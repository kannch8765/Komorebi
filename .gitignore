# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Virtual envs
.venv/
venv/
ENV/

# Secrets / local config
.env
.env.*
!.env.example

# Project-specific: CLAUDE.md is local-only from this commit onwards
CLAUDE.md

# Claude Code hook config + scripts are local-only, never pushed.
# Each developer wires their own .claude/settings.json for their machine.
# The hook scripts are paired with that config; if one is local, both are.
.claude/
scripts/doc_check_hook.sh
scripts/post_push_test.sh

# Local-only runtime data — NEVER commit:
# - data/places_api_usage.json: Google Places API call counter
#   (best-effort disk writes from tools/places_api.py)
# - data/user_profile.json: the user's home location + future personal
#   context (PII; see models/user_profile.py)
data/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db